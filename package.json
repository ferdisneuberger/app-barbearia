{
  "name": "projetinho-barbearia",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace api",
    "dev:web": "npm run dev --workspace web",
    "test:api": "npm run test --workspace api",
    "build:web": "npm run build --workspace web"
  }
}
